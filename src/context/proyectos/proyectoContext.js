//ProyectoContext y proyectoReducer vienen a sustituir a Redux sin agregar nada, viene en React

import { createContext } from "react";

const proyectoContext = createContext();

export default proyectoContext;
